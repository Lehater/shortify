package shortify;

import shortify.controller.CommandLineController;

public class Shortify {
    public static void main(String[] args) {
        CommandLineController commandLineController = new CommandLineController();
        commandLineController.start();
    }
}

